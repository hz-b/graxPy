# How-To Guides

This section is reserved for focused recipes. It is intentionally empty in the
initial documentation set so the navigation structure is stable before adding
task-specific guides.

When a recipe launches simulations, you can set `polarization` explicitly with
either `s` or `p`. The simulation-oriented guides here use explicit
`polarization="p"` so the workflow is fully spelled out.

```{toctree}
:maxdepth: 1

custom-grating-profiles
user-defined-cases
```
